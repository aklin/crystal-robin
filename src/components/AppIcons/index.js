import React from 'react';
import { AcUnit, Whatshot } from '@material-ui/icons';

export const HotIcon = (props) => <Whatshot {...props} />;
export const ColdIcon = (props) => <AcUnit {...props} />;
